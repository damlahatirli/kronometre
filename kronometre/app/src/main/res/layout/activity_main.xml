<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:id="@+id/relativeLayout"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="150dp"
        android:layout_marginTop="70dp"
        android:layout_marginEnd="150dp"
        android:text="0"
        android:textColor="#ffffff"
        android:textSize="65sp"></TextView>

    <ImageButton
        android:id="@+id/basla"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:background="@drawable/start"
        app:srcCompat="@drawable/start"></ImageButton>

    <ImageButton
        android:id="@+id/durdur"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_below="@id/basla"
        android:layout_marginLeft="250dp"
        android:layout_marginBottom="20dp"
        android:background="@drawable/pause"></ImageButton>

    <ImageButton
        android:id="@+id/sifirla"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginLeft="30dp"
        android:layout_below="@id/basla"
        android:layout_marginBottom="20dp"
        android:background="@drawable/reset"></ImageButton>





</RelativeLayout>